<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Android 12+ displays this as the single initial splash. MainActivity
         skips the Compose splash on these releases to avoid a duplicate frame. -->
    <style name="Theme.BlazaresOrpheus" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/orpheus_splash_figure</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
    </style>
</resources>
